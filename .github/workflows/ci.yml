name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test-ubuntu-x64-gnu:
    name: Build and test Ubuntu x64
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
        with:
          version: 11.20.0
          runtime: node@22.18.0
          cache: true
          install: false
      - uses: dtolnay/rust-toolchain@stable
      - run: pnpm install --frozen-lockfile
      - name: Build and run all tests
        run: pnpm exec vp run check:test
      - name: Verify committed package outputs are current
        run: git diff --exit-code -- packages/taffyjs-node/index.js packages/taffyjs-node/index.d.ts packages/taffyjs-node/binding.js packages/taffyjs-node/binding.d.ts

  test-windows-x64-msvc:
    name: Build and test Windows x64
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
        with:
          version: 11.20.0
          runtime: node@22.18.0
          cache: true
          install: false
      - uses: dtolnay/rust-toolchain@stable
      - run: pnpm install --frozen-lockfile
      - name: Build and run all tests
        run: pnpm exec vp run check:test

  test-wasm:
    name: Build and test WASIP
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
        with:
          version: 11.20.0
          runtime: node@22.18.0
          cache: true
          install: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip1
      - run: pnpm install --frozen-lockfile
      - name: Install the browser used by the Wasm runtime test
        run: pnpm --filter @taffyjs/wasm-integration-tests exec playwright install --with-deps chromium
      - name: Build and test the Wasm package
        run: pnpm exec vp run check:wasm
      - name: Verify committed Wasm package outputs are current
        run: git diff --exit-code -- packages/taffyjs-wasm/dist/index.js packages/taffyjs-wasm/dist/index.browser.js packages/taffyjs-wasm/dist/index.d.ts packages/taffyjs-wasm/dist/taffyjs.browser.js packages/taffyjs-wasm/dist/taffyjs.node.cjs packages/taffyjs-wasm/dist/taffyjs.node.js packages/taffyjs-wasm/dist/taffyjs.wasm-base64.js packages/taffyjs-wasm/dist/taffyjs.wasip1-deferred.js

  check-node:
    name: Node checks
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
        with:
          version: 11.20.0
          runtime: node@22.18.0
          cache: true
          install: false
      - run: pnpm install --frozen-lockfile
      - name: Check formatting
        run: pnpm exec vp run check:format
      - name: Check JavaScript and TypeScript
        run: pnpm exec vp run check:lint
      - name: Verify generated sources are committed
        run: pnpm exec vp run check:codegen

  check-rust:
    name: Rust checks
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
