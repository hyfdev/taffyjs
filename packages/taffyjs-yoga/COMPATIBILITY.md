# @taffyjs/yoga Compatibility

The compatibility baseline is exactly `yoga-layout@3.2.1`. The implementation is being completed through reviewed milestones; the exhaustive Compatible, Different, and Unsupported matrix will be finalized before this package leaves draft status.

The package is permanently Node-only and ESM-only. It does not provide a browser or WASM backend.
