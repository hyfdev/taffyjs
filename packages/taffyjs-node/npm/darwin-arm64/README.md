# @taffyjs/binding-darwin-arm64

Private platform package for the `@taffyjs/node` macOS ARM64 native artifact (`aarch64-apple-darwin`). The package build adds the binary; publication is not configured.
