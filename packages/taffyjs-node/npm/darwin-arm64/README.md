# @taffyjs/binding-darwin-arm64

Published platform implementation for the `@taffyjs/node` macOS ARM64 native artifact (`aarch64-apple-darwin`). Consumers install `@taffyjs/node`; do not import this package directly.
