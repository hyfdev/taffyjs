# @taffyjs/binding-android-arm-eabi

Private platform package for the `@taffyjs/node` Android ARMv7 native artifact (`armv7-linux-androideabi`). The package build adds the binary; publication is not configured.
