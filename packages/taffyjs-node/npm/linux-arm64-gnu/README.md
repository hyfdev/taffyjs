# @taffyjs/binding-linux-arm64-gnu

Private platform package for the `@taffyjs/node` Linux ARM64 GNU native artifact (`aarch64-unknown-linux-gnu`). The package build adds the binary; publication is not configured.
