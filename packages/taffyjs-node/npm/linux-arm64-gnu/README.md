# @taffyjs/binding-linux-arm64-gnu

Published platform implementation for the `@taffyjs/node` Linux ARM64 GNU native artifact (`aarch64-unknown-linux-gnu`). Consumers install `@taffyjs/node`; do not import this package directly.
