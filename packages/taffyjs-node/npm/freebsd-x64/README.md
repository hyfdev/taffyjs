# @taffyjs/binding-freebsd-x64

Published platform implementation for the `@taffyjs/node` FreeBSD x64 native artifact (`x86_64-unknown-freebsd`). Consumers install `@taffyjs/node`; do not import this package directly.
