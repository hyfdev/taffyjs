# @taffyjs/binding-freebsd-x64

Private platform package for the `@taffyjs/node` FreeBSD x64 native artifact (`x86_64-unknown-freebsd`). The package build adds the binary; publication is not configured.
