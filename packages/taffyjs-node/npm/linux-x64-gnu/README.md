# @taffyjs/binding-linux-x64-gnu

Private platform package for the `@taffyjs/node` Linux x64 GNU native artifact (`x86_64-unknown-linux-gnu`). The package build adds the binary; publication is not configured.
