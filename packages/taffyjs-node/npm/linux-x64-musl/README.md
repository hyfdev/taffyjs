# @taffyjs/binding-linux-x64-musl

Private platform package for the `@taffyjs/node` Linux x64 musl native artifact (`x86_64-unknown-linux-musl`). The package build adds the binary; publication is not configured.
