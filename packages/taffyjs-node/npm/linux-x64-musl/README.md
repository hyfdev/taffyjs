# @taffyjs/binding-linux-x64-musl

Published platform implementation for the `@taffyjs/node` Linux x64 musl native artifact (`x86_64-unknown-linux-musl`). Consumers install `@taffyjs/node`; do not import this package directly.
