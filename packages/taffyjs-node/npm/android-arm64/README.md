# @taffyjs/binding-android-arm64

Published platform implementation for the `@taffyjs/node` Android ARM64 native artifact (`aarch64-linux-android`). Consumers install `@taffyjs/node`; do not import this package directly.
