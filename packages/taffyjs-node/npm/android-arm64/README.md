# @taffyjs/binding-android-arm64

Private platform package for the `@taffyjs/node` Android ARM64 native artifact (`aarch64-linux-android`). The package build adds the binary; publication is not configured.
