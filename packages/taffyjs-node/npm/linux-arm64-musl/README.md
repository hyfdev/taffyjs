# @taffyjs/binding-linux-arm64-musl

Published platform implementation for the `@taffyjs/node` Linux ARM64 musl native artifact (`aarch64-unknown-linux-musl`). Consumers install `@taffyjs/node`; do not import this package directly.
