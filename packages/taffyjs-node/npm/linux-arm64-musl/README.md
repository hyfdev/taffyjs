# @taffyjs/binding-linux-arm64-musl

Private platform package for the `@taffyjs/node` Linux ARM64 musl native artifact (`aarch64-unknown-linux-musl`). The package build adds the binary; publication is not configured.
