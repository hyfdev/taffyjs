# @taffyjs/binding-linux-arm-gnueabihf

Private platform package for the `@taffyjs/node` Linux ARMv7 GNU native artifact (`armv7-unknown-linux-gnueabihf`). The package build adds the binary; publication is not configured.
