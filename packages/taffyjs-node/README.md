# `@taffyjs/node`

`@taffyjs/node` is the ESM-only native Node-API entry point for TaffyJS. Its napi-rs-generated loader selects an optional `@taffyjs/binding-<platform>` package without an intermediate generic binding package. The package is private while the binding API and supported release targets are being designed.
